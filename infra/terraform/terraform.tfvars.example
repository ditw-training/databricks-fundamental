# Copy to terraform.tfvars (git-ignored) and fill in.

subscription_id = "00000000-0000-0000-0000-000000000000"
location        = "westeurope"
prefix          = "dbxfund"

# ── Lab tenant: deploy into a prepared resource group where you are Owner ──
# resource_group_name         = "DBX-FE_140926"
# register_resource_providers = false

# ── Participants: change per training, then plan + apply. E-mails only, never passwords. ──
participants = [
  # "student501@example.onmicrosoft.com",
]

# ── Optional: account-level (requires Azure Databricks account admin) ──
# databricks_account_id = "00000000-0000-0000-0000-000000000000"
# metastore_id          = "00000000-0000-0000-0000-000000000000" # only if new workspaces are not auto-assigned
# create_analysts_group = true
